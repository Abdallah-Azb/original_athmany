import 'package:flutter/material.dart';

class PaidContainer extends StatelessWidget {
  const PaidContainer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ClipRRect(
      child: Text('1111'),
    );
  }
}
